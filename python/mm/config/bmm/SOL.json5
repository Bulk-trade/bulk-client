{
    // coin to be MM (will be rendered to proper bulk and binance perp symbol)
    coin: "SOL",
    // API (for bulkstar: "ws://localhost:12001")
    bulk_ws_url: "wss://exchange-wss.bulk.trade",

    // order size increment
    chunk_size: 50.0,
    // Maximum number of new orders per level
    max_new_orders: 5,
    // Maximum number of price levels to replicate
    max_price_levels: 100,

    // tick resolution (in price) for the 1st K levels
    fine_tick: 0.10,
    // tick resolution (in price) for the deeper levels
    coarse_tick: 1.0,
    // maximum depth (bps)
    max_depth: 1000.0,

    // Update frequency (seconds between syncs)
    sync_interval: 0.250,
    // Seconds between monitoring checks
    monitor_interval: 1.0,

    // Maximum inventory before squaring
    max_inventory_notional: 250000000.0,
    // Amount of inventory to close when above threshold
    inventory_close_fraction: 0.50,
    // max depth in closing inventory (bps)
    inventory_close_depth: 15.0,

    // Binance monitoring (seconds without update before reconnect)
    binance_timeout: 60.0,

    // logging
    log_level: "INFO",
    // what to log: ["STATE", "FILL", "POSITION"]
    to_log: ["FILL", "POSITION"],

    // indicate whether simulated or hitting bulk
    simulated: false,
}
